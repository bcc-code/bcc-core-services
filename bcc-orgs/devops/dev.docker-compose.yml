version: '3.1'

services:
  #################
  # Application   #
  #################
  person_app:
    ports:
      - "4000:4000"
    volumes:
      - ../src:/app/src
    env_file: ../.env
    build: 
      context: ../
      dockerfile: devops/dev.Dockerfile