version: "1"
packages:
  - name: "sqlc"
    path: "src/sqlc"
    queries: "./db/queries/"
    schema: "./db/migrations/"
    engine: "postgresql"
    emit_db_tags: true
overrides:
  - go_type: "github.com/gofrs/uuid.UUID"
    db_type: varchar

